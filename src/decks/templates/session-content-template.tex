% Save beside the session wrapper as session-01-name-content.tex.
% This fragment is shared by the standalone session and the assembled course deck.
% Do not compile it directly.
\SessionDivider{01}{First session}{One concise statement of the session outcome.}
\SessionTopicMapSlide{
  \TopicEntry{01}{First topic}{The first independently buildable learning unit.}
}
\import{topics/}{topic-01-name-content.tex}
